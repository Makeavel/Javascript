let valor; // não inicializada (sem valor inicial declarado)

console.log(valor); // -> retorna Undefined 

valor = null; // ausencia de valor (o null aqui foi definido por mim...)

console.log(valor);

let validade;

// console.log(validade.toString()); //vai dar erro

validade = null

const palavra = {}

console.log(palavra.frase); //undefined porque frase não foi definida

palavra.frase = "A vida é bela";

console.log(palavra.frase);